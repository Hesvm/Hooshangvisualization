<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# UI polish is mandatory

For ANY work that touches the user interface — building components, styling, animations, or reviewing/editing frontend code — you MUST invoke the `make-interfaces-feel-better` skill and apply its principles (concentric border radius, optical alignment, shadows over borders, interruptible/staggered animations, tabular numbers, text-wrap, font smoothing, image outlines, scale-on-press, no `transition: all`, minimum hit areas). This is not optional and applies to every UI change in this project.
