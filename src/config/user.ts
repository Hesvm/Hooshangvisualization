/**
 * Hardcoded for v1 — no auth/profile page exists yet. Confirm whether this
 * should instead read from a profile config once that page is built.
 */
export const currentUser = {
  name: "حسام",
};
